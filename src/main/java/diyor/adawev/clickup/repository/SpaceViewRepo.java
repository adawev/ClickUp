package diyor.adawev.clickup.repository;

import diyor.adawev.clickup.model.SpaceView;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SpaceViewRepo extends JpaRepository<SpaceView, Integer> {
}
